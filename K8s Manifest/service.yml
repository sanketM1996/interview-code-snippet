apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app.kubernetes.io/name: nginx

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: nginx

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP